# Example environment configuration file
# Copy this file and rename it to ".env" before running the project.

# Application runtime environment
NODE_ENV=development

# Server port (default example)
PORT=3000

# Application name
APP_NAME=enterprise-ecommerce-backend

# Logging level used by Winston logger
# Recommended default for most environments
LOG_LEVEL=info